---
import { getLocalizedAppHref, resolveLocaleRoute } from "../i18n/routing";
import BaseLayout from "../layouts/BaseLayout.astro";
import { getCurrentLocale } from "../lib/i18n";

const currentLocale = getCurrentLocale();
const currentPathname = Astro.originPathname || Astro.url.pathname;
const currentRoute =
  Astro.locals.localeRoute ?? resolveLocaleRoute(currentPathname);
const localizeAppHref = (href: string) =>
  getLocalizedAppHref(href, currentRoute.language.slug);
const homeHref = localizeAppHref("/");
const discoverHref = localizeAppHref("/discover");
const tryOnHref = localizeAppHref("/tryon-free");
const tryOnProHref = localizeAppHref("/tryon-pro");
const tryOnPremiumHref = localizeAppHref("/tryon-premium");
const shareCardHref = localizeAppHref("/share-card");
const shareBaseHref = localizeAppHref("/share");

type ShareCardCopy = {
  title: string;
  description: string;
  navLabel: string;
  navTitle: string;
  navHome: string;
  navDiscover: string;
  navTryOn: string;
  navTryOnPaid: string;
  navTryCta: string;
  navTryShort: string;
  cardAria: string;
  heading: string;
  loading: string;
  cardLoading: string;
  benefitTryon: string;
  benefitTryonDesc: string;
  benefitScenario: string;
  benefitScenarioDesc: string;
  benefitFree: string;
  benefitFreeDesc: string;
  ctaTitle: string;
  ctaHighlight: string;
  ctaBody: string;
  download: string;
  cleanDownload?: string;
  share: string;
  copyLink: string;
  reward: string;
  emptyTitle: string;
  emptyBody: string;
  emptyCta: string;
  emptyHome: string;
  defaultLook: string;
  resultAltSuffix: string;
  originalAltSuffix: string;
  before: string;
  after: string;
  shareTitle: string;
  shareText: string;
  shareCopied: string;
  copied: string;
  rewardSuccess: string;
  rewardAlready: string;
  rewardFailed: string;
};

const shareCopyByLocale = {
  en: {
    title: "My try-on result | AI Beauty Stylist",
    description:
      "View and share an AI Beauty Stylist makeup try-on reference result.",
    navLabel: "Share page navigation",
    navTitle: "Share result",
    navHome: "Home",
    navDiscover: "Discover looks",
    navTryOn: "Free try-on",
    navTryOnPaid: "Try-on",
    navTryCta: "Try this look",
    navTryShort: "Try this",
    cardAria: "Makeup try-on share card",
    heading: "My try-on result",
    loading: "Loading...",
    cardLoading: "Loading share card...",
    benefitTryon: "AI makeup try-on",
    benefitTryonDesc: "Preview the look on your own face",
    benefitScenario: "Looks by scenario",
    benefitScenarioDesc: "Try work, date, and photo-ready looks",
    benefitFree: "Daily bonus",
    benefitFreeDesc: "Share once daily for +1 generation",
    ctaTitle: "Free try-ons every day",
    ctaHighlight: "Share once daily to unlock one more AI makeup generation.",
    ctaBody: "Create a real before-and-after card to save or share.",
    download: "Save share card",
    cleanDownload: "Download clean try-on image",
    share: "Share card",
    copyLink: "Copy link",
    reward: "Get share reward (+1 credit)",
    emptyTitle: "No try-on result yet",
    emptyBody: "Finish a try-on first, then generate a shareable result.",
    emptyCta: "Try makeup",
    emptyHome: "Back home",
    defaultLook: "Makeup result",
    resultAltSuffix: " try-on result",
    originalAltSuffix: " original selfie",
    before: "Before",
    after: "After",
    shareTitle: "My AI makeup try-on result",
    shareText: "See my AI makeup try-on reference result.",
    shareCopied: "✓ Link copied",
    copied: "✓ Copied",
    rewardSuccess: "✓ Credit +1 reward claimed",
    rewardAlready: "Already claimed today",
    rewardFailed: "× Reward failed. Try again",
  },
  "zh-CN": {
    title: "我的试妆效果 | AI Beauty Stylist",
    description: "查看并分享 AI Beauty Stylist 生成的试妆参考效果。",
    navLabel: "分享页导航",
    navTitle: "分享结果",
    navHome: "首页",
    navDiscover: "发现妆容",
    navTryOn: "免费试妆",
    navTryOnPaid: "试妆",
    navTryCta: "试试这个妆",
    navTryShort: "试妆",
    cardAria: "试妆分享卡",
    heading: "我的试妆效果",
    loading: "加载中…",
    cardLoading: "加载分享卡中…",
    benefitTryon: "AI 在线试妆",
    benefitTryonDesc: "先看自己的上脸参考",
    benefitScenario: "按场景选妆",
    benefitScenarioDesc: "通勤、约会、拍照都能试",
    benefitFree: "每日奖励",
    benefitFreeDesc: "每天分享一次，加 1 次生成额度",
    ctaTitle: "每天都有免费试妆",
    ctaHighlight: "每天分享一次，可解锁 1 次 AI 妆容生成。",
    ctaBody: "生成真实前后对比图，适合保存和分享。",
    download: "保存分享卡",
    cleanDownload: "下载纯试妆图",
    share: "分享卡片",
    copyLink: "复制链接",
    reward: "获取分享奖励 (+1 额度)",
    emptyTitle: "暂无试妆结果",
    emptyBody: "先完成一次试妆，再生成可分享的结果。",
    emptyCta: "去试妆",
    emptyHome: "返回首页",
    defaultLook: "妆容效果",
    resultAltSuffix: "试妆效果",
    originalAltSuffix: "原始自拍",
    before: "原图",
    after: "试妆后",
    shareTitle: "我的 AI 试妆效果",
    shareText: "来看看我的 AI 试妆参考效果。",
    shareCopied: "✓ 链接已复制",
    copied: "✓ 已复制",
    rewardSuccess: "✓ 额度 +1，奖励已领取！",
    rewardAlready: "今日已领取",
    rewardFailed: "× 领取失败，请重试",
  },
  "zh-TW": {
    title: "我的試妝效果 | AI Beauty Stylist",
    description: "查看並分享 AI Beauty Stylist 生成的試妝參考效果。",
    navLabel: "分享頁導覽",
    navTitle: "分享結果",
    navHome: "首頁",
    navDiscover: "發現妝容",
    navTryOn: "免費試妝",
    navTryOnPaid: "試妝",
    navTryCta: "試試這個妝",
    navTryShort: "試妝",
    cardAria: "試妝分享卡",
    heading: "我的試妝效果",
    loading: "載入中…",
    cardLoading: "正在載入分享卡…",
    benefitTryon: "AI 線上試妝",
    benefitTryonDesc: "先看自己的上臉參考",
    benefitScenario: "按場景選妝",
    benefitScenarioDesc: "通勤、約會、拍照都能試",
    benefitFree: "每日獎勵",
    benefitFreeDesc: "每天分享一次，加 1 次生成額度",
    ctaTitle: "每天都有免費試妝",
    ctaHighlight: "每天分享一次，可解鎖 1 次 AI 妝容生成。",
    ctaBody: "生成真實前後對比圖，適合保存和分享。",
    download: "保存分享卡",
    cleanDownload: "下載純試妝圖",
    share: "分享卡片",
    copyLink: "複製連結",
    reward: "領取分享獎勵 (+1 額度)",
    emptyTitle: "暫無試妝結果",
    emptyBody: "先完成一次試妝，再生成可分享的結果。",
    emptyCta: "去試妝",
    emptyHome: "返回首頁",
    defaultLook: "妝容效果",
    resultAltSuffix: "試妝效果",
    originalAltSuffix: "原始自拍",
    before: "原圖",
    after: "試妝後",
    shareTitle: "我的 AI 試妝效果",
    shareText: "來看看我的 AI 試妝參考效果。",
    shareCopied: "✓ 連結已複製",
    copied: "✓ 已複製",
    rewardSuccess: "✓ 額度 +1，獎勵已領取！",
    rewardAlready: "今日已領取",
    rewardFailed: "× 領取失敗，請重試",
  },
  "de-DE": {
    title: "Mein Try-on-Ergebnis | AI Beauty Stylist",
    description:
      "Sieh dir ein von AI Beauty Stylist erzeugtes Make-up-Try-on-Ergebnis an und teile es.",
    navLabel: "Navigation der Teilen-Seite",
    navTitle: "Ergebnis teilen",
    navHome: "Startseite",
    navDiscover: "Looks entdecken",
    navTryOn: "Kostenlos testen",
    navTryOnPaid: "Testen",
    navTryCta: "Diesen Look testen",
    navTryShort: "Testen",
    cardAria: "Make-up-Try-on-Teilen-Karte",
    heading: "Mein Try-on-Ergebnis",
    loading: "Lädt...",
    cardLoading: "Teilen-Karte wird geladen...",
    benefitTryon: "AI Make-up-Try-on",
    benefitTryonDesc: "Den Look zuerst am eigenen Gesicht ansehen",
    benefitScenario: "Looks nach Anlass",
    benefitScenarioDesc: "Für Arbeit, Date und Fotos testen",
    benefitFree: "Tagesbonus",
    benefitFreeDesc: "Einmal täglich teilen für +1 Generierung",
    ctaTitle: "Jeden Tag kostenlose Try-ons",
    ctaHighlight:
      "Teile einmal täglich und schalte eine weitere AI Make-up-Generierung frei.",
    ctaBody:
      "Erstelle eine echte Vorher-nachher-Karte zum Speichern oder Teilen.",
    download: "Teilen-Karte speichern",
    cleanDownload: "Reines Try-on-Bild herunterladen",
    share: "Karte teilen",
    copyLink: "Link kopieren",
    reward: "Teilen-Bonus erhalten (+1 Credit)",
    emptyTitle: "Noch kein Try-on-Ergebnis",
    emptyBody:
      "Schließe zuerst ein Try-on ab, dann kannst du ein teilbares Ergebnis erstellen.",
    emptyCta: "Make-up testen",
    emptyHome: "Zur Startseite",
    defaultLook: "Make-up-Ergebnis",
    resultAltSuffix: " Try-on-Ergebnis",
    originalAltSuffix: " Original-Selfie",
    before: "Vorher",
    after: "Nachher",
    shareTitle: "Mein AI Make-up-Try-on-Ergebnis",
    shareText: "Sieh dir mein AI Make-up-Try-on-Ergebnis an.",
    shareCopied: "✓ Link kopiert",
    copied: "✓ Kopiert",
    rewardSuccess: "✓ Credit +1, Bonus erhalten",
    rewardAlready: "Heute bereits erhalten",
    rewardFailed: "× Bonus fehlgeschlagen. Erneut versuchen",
  },
  "fr-FR": {
    title: "Mon résultat d'essai | AI Beauty Stylist",
    description:
      "Consultez et partagez un résultat d'essai maquillage généré par AI Beauty Stylist.",
    navLabel: "Navigation de la page de partage",
    navTitle: "Résultat partagé",
    navHome: "Accueil",
    navDiscover: "Découvrir les looks",
    navTryOn: "Essai gratuit",
    navTryOnPaid: "Essai",
    navTryCta: "Essayer ce look",
    navTryShort: "Essayer",
    cardAria: "Carte de partage d'essai maquillage",
    heading: "Mon résultat d'essai",
    loading: "Chargement…",
    cardLoading: "Chargement de la carte…",
    benefitTryon: "Essai maquillage IA",
    benefitTryonDesc: "Prévisualisez le look sur votre visage",
    benefitScenario: "Looks par occasion",
    benefitScenarioDesc: "Travail, rendez-vous et photos",
    benefitFree: "Bonus quotidien",
    benefitFreeDesc: "Partagez chaque jour pour +1 génération",
    ctaTitle: "Des essais gratuits chaque jour",
    ctaHighlight:
      "Partagez une fois par jour pour débloquer une génération maquillage IA en plus.",
    ctaBody: "Créez une vraie carte avant/après à enregistrer ou partager.",
    download: "Enregistrer la carte",
    cleanDownload: "Télécharger l'image seule",
    share: "Partager la carte",
    copyLink: "Copier le lien",
    reward: "Obtenir la récompense (+1 crédit)",
    emptyTitle: "Aucun résultat d'essai",
    emptyBody:
      "Terminez d'abord un essai, puis générez un résultat partageable.",
    emptyCta: "Essayer le maquillage",
    emptyHome: "Retour à l'accueil",
    defaultLook: "Résultat maquillage",
    resultAltSuffix: " résultat d'essai",
    originalAltSuffix: " selfie original",
    before: "Avant",
    after: "Après",
    shareTitle: "Mon résultat d'essai maquillage IA",
    shareText: "Découvrez mon résultat d'essai maquillage IA.",
    shareCopied: "✓ Lien copié",
    copied: "✓ Copié",
    rewardSuccess: "✓ Crédit +1, récompense obtenue",
    rewardAlready: "Déjà obtenue aujourd'hui",
    rewardFailed: "× Échec. Réessayez",
  },
  "ja-JP": {
    title: "私のメイク試着結果 | AI Beauty Stylist",
    description:
      "AI Beauty Stylist が生成したメイク試着の参考結果を表示・共有できます。",
    navLabel: "共有ページのナビゲーション",
    navTitle: "共有結果",
    navHome: "ホーム",
    navDiscover: "メイクを探す",
    navTryOn: "無料試着",
    navTryOnPaid: "試着",
    navTryCta: "このルックを試す",
    navTryShort: "試す",
    cardAria: "メイク試着共有カード",
    heading: "私のメイク試着結果",
    loading: "読み込み中…",
    cardLoading: "共有カードを読み込み中…",
    benefitTryon: "AIメイク試着",
    benefitTryonDesc: "自分の顔で仕上がりを確認",
    benefitScenario: "シーン別メイク",
    benefitScenarioDesc: "通勤・デート・写真用も試せます",
    benefitFree: "毎日ボーナス",
    benefitFreeDesc: "1日1回のシェアで生成を1回追加",
    ctaTitle: "毎日無料で試せる",
    ctaHighlight:
      "1日1回シェアすると、AIメイク生成をもう1回アンロックできます。",
    ctaBody: "保存や共有に使えるリアルなビフォーアフターを作成します。",
    download: "共有カードを保存",
    cleanDownload: "試着画像のみをダウンロード",
    share: "カードを共有",
    copyLink: "リンクをコピー",
    reward: "共有特典を受け取る（+1クレジット）",
    emptyTitle: "試着結果がありません",
    emptyBody: "先に試着を完了すると、共有できる結果を生成できます。",
    emptyCta: "メイクを試す",
    emptyHome: "ホームへ戻る",
    defaultLook: "メイク結果",
    resultAltSuffix: "の試着結果",
    originalAltSuffix: "の元のセルフィー",
    before: "元画像",
    after: "試着後",
    shareTitle: "私のAIメイク試着結果",
    shareText: "私のAIメイク試着の参考結果を見てください。",
    shareCopied: "✓ リンクをコピーしました",
    copied: "✓ コピーしました",
    rewardSuccess: "✓ クレジット +1、特典を受け取りました",
    rewardAlready: "本日は受け取り済み",
    rewardFailed: "× 受け取りに失敗しました。再試行してください",
  },
  "ko-KR": {
    title: "내 메이크업 체험 결과 | AI Beauty Stylist",
    description:
      "AI Beauty Stylist가 생성한 메이크업 체험 참고 결과를 보고 공유하세요.",
    navLabel: "공유 페이지 내비게이션",
    navTitle: "공유 결과",
    navHome: "홈",
    navDiscover: "룩 둘러보기",
    navTryOn: "무료 체험",
    navTryOnPaid: "체험",
    navTryCta: "이 룩 체험하기",
    navTryShort: "체험",
    cardAria: "메이크업 체험 공유 카드",
    heading: "내 메이크업 체험 결과",
    loading: "불러오는 중…",
    cardLoading: "공유 카드 불러오는 중…",
    benefitTryon: "AI 메이크업 체험",
    benefitTryonDesc: "내 얼굴에 적용된 모습을 미리 확인",
    benefitScenario: "상황별 룩",
    benefitScenarioDesc: "출근, 데이트, 촬영 룩을 체험",
    benefitFree: "매일 보너스",
    benefitFreeDesc: "하루 한 번 공유하면 생성 1회 추가",
    ctaTitle: "매일 무료 체험",
    ctaHighlight:
      "하루 한 번 공유하면 AI 메이크업 생성 1회를 더 받을 수 있어요.",
    ctaBody: "저장하고 공유하기 좋은 실제 전후 비교 카드를 만듭니다.",
    download: "공유 카드 저장",
    cleanDownload: "순수 시착 이미지 다운로드",
    share: "카드 공유",
    copyLink: "링크 복사",
    reward: "공유 보상 받기 (+1 크레딧)",
    emptyTitle: "체험 결과 없음",
    emptyBody: "먼저 체험을 완료하면 공유 가능한 결과를 만들 수 있습니다.",
    emptyCta: "메이크업 체험",
    emptyHome: "홈으로",
    defaultLook: "메이크업 결과",
    resultAltSuffix: " 체험 결과",
    originalAltSuffix: " 원본 셀피",
    before: "원본",
    after: "체험 후",
    shareTitle: "내 AI 메이크업 체험 결과",
    shareText: "내 AI 메이크업 체험 참고 결과를 확인해 보세요.",
    shareCopied: "✓ 링크가 복사되었습니다",
    copied: "✓ 복사됨",
    rewardSuccess: "✓ 크레딧 +1, 보상을 받았습니다",
    rewardAlready: "오늘 이미 받았습니다",
    rewardFailed: "× 보상 받기 실패. 다시 시도하세요",
  },
  "es-ES": {
    title: "Mi resultado de prueba | AI Beauty Stylist",
    description:
      "Mira y comparte un resultado de referencia de prueba de maquillaje generado por AI Beauty Stylist.",
    navLabel: "Navegación de la página compartida",
    navTitle: "Resultado compartido",
    navHome: "Inicio",
    navDiscover: "Descubrir looks",
    navTryOn: "Prueba gratis",
    navTryOnPaid: "Probar",
    navTryCta: "Probar este look",
    navTryShort: "Probar",
    cardAria: "Tarjeta compartida de prueba de maquillaje",
    heading: "Mi resultado de prueba",
    loading: "Cargando...",
    cardLoading: "Cargando tarjeta...",
    benefitTryon: "Prueba de maquillaje IA",
    benefitTryonDesc: "Previsualiza el look en tu rostro",
    benefitScenario: "Looks por ocasión",
    benefitScenarioDesc: "Trabajo, cita y fotos",
    benefitFree: "Bonus diario",
    benefitFreeDesc: "Comparte una vez al día para +1 generación",
    ctaTitle: "Pruebas gratis todos los días",
    ctaHighlight:
      "Comparte una vez al día para desbloquear una generación de maquillaje IA más.",
    ctaBody:
      "Crea una tarjeta real de antes y después para guardar o compartir.",
    download: "Guardar tarjeta",
    cleanDownload: "Descargar imagen limpia",
    share: "Compartir tarjeta",
    copyLink: "Copiar enlace",
    reward: "Obtener recompensa (+1 crédito)",
    emptyTitle: "Aún no hay resultado de prueba",
    emptyBody:
      "Completa primero una prueba y luego genera un resultado compartible.",
    emptyCta: "Probar maquillaje",
    emptyHome: "Volver al inicio",
    defaultLook: "Resultado de maquillaje",
    resultAltSuffix: " resultado de prueba",
    originalAltSuffix: " selfie original",
    before: "Antes",
    after: "Después",
    shareTitle: "Mi resultado de maquillaje IA",
    shareText: "Mira mi resultado de referencia de maquillaje IA.",
    shareCopied: "✓ Enlace copiado",
    copied: "✓ Copiado",
    rewardSuccess: "✓ Crédito +1, recompensa recibida",
    rewardAlready: "Ya recibida hoy",
    rewardFailed: "× No se pudo recibir. Inténtalo de nuevo",
  },
  "es-419": {
    title: "Mi resultado de prueba | AI Beauty Stylist",
    description:
      "Mira y comparte un resultado de referencia de prueba de maquillaje generado por AI Beauty Stylist.",
    navLabel: "Navegación de la página compartida",
    navTitle: "Resultado compartido",
    navHome: "Inicio",
    navDiscover: "Descubrir looks",
    navTryOn: "Prueba gratis",
    navTryOnPaid: "Probar",
    navTryCta: "Probar este look",
    navTryShort: "Probar",
    cardAria: "Tarjeta compartida de prueba de maquillaje",
    heading: "Mi resultado de prueba",
    loading: "Cargando...",
    cardLoading: "Cargando tarjeta...",
    benefitTryon: "Prueba de maquillaje IA",
    benefitTryonDesc: "Previsualiza el look en tu rostro",
    benefitScenario: "Looks por ocasión",
    benefitScenarioDesc: "Trabajo, cita y fotos",
    benefitFree: "Bonus diario",
    benefitFreeDesc: "Comparte una vez al día para +1 generación",
    ctaTitle: "Pruebas gratis todos los días",
    ctaHighlight:
      "Comparte una vez al día para desbloquear una generación de maquillaje IA más.",
    ctaBody:
      "Crea una tarjeta real de antes y después para guardar o compartir.",
    download: "Guardar tarjeta",
    cleanDownload: "Descargar imagen limpia",
    share: "Compartir tarjeta",
    copyLink: "Copiar enlace",
    reward: "Obtener recompensa (+1 crédito)",
    emptyTitle: "Aún no hay resultado de prueba",
    emptyBody:
      "Completa primero una prueba y luego genera un resultado compartible.",
    emptyCta: "Probar maquillaje",
    emptyHome: "Volver al inicio",
    defaultLook: "Resultado de maquillaje",
    resultAltSuffix: " resultado de prueba",
    originalAltSuffix: " selfie original",
    before: "Antes",
    after: "Después",
    shareTitle: "Mi resultado de maquillaje IA",
    shareText: "Mira mi resultado de referencia de maquillaje IA.",
    shareCopied: "✓ Enlace copiado",
    copied: "✓ Copiado",
    rewardSuccess: "✓ Crédito +1, recompensa recibida",
    rewardAlready: "Ya recibida hoy",
    rewardFailed: "× No se pudo recibir. Inténtalo de nuevo",
  },
  "pt-BR": {
    title: "Meu resultado de teste | AI Beauty Stylist",
    description:
      "Veja e compartilhe um resultado de referência de teste de maquiagem gerado pelo AI Beauty Stylist.",
    navLabel: "Navegação da página de compartilhamento",
    navTitle: "Resultado compartilhado",
    navHome: "Início",
    navDiscover: "Descobrir looks",
    navTryOn: "Teste grátis",
    navTryOnPaid: "Testar",
    navTryCta: "Testar este look",
    navTryShort: "Testar",
    cardAria: "Cartão de compartilhamento do teste de maquiagem",
    heading: "Meu resultado de teste",
    loading: "Carregando...",
    cardLoading: "Carregando cartão...",
    benefitTryon: "Teste de maquiagem IA",
    benefitTryonDesc: "Pré-visualize o look no seu rosto",
    benefitScenario: "Looks por ocasião",
    benefitScenarioDesc: "Teste trabalho, encontro e fotos",
    benefitFree: "Bônus diário",
    benefitFreeDesc: "Compartilhe uma vez ao dia para +1 geração",
    ctaTitle: "Testes grátis todos os dias",
    ctaHighlight:
      "Compartilhe uma vez por dia para liberar mais uma geração de maquiagem IA.",
    ctaBody:
      "Crie um cartão real de antes e depois para salvar ou compartilhar.",
    download: "Salvar cartão",
    cleanDownload: "Baixar imagem limpa",
    share: "Compartilhar cartão",
    copyLink: "Copiar link",
    reward: "Receber recompensa (+1 crédito)",
    emptyTitle: "Ainda não há resultado de teste",
    emptyBody:
      "Conclua um teste primeiro para gerar um resultado compartilhável.",
    emptyCta: "Testar maquiagem",
    emptyHome: "Voltar ao início",
    defaultLook: "Resultado de maquiagem",
    resultAltSuffix: " resultado do teste",
    originalAltSuffix: " selfie original",
    before: "Antes",
    after: "Depois",
    shareTitle: "Meu resultado de maquiagem IA",
    shareText: "Veja meu resultado de referência de maquiagem IA.",
    shareCopied: "✓ Link copiado",
    copied: "✓ Copiado",
    rewardSuccess: "✓ Crédito +1, recompensa recebida",
    rewardAlready: "Já recebida hoje",
    rewardFailed: "× Falha ao receber. Tente novamente",
  },
} satisfies Record<string, ShareCardCopy>;

const copy = shareCopyByLocale[currentLocale] ?? shareCopyByLocale.en;

const posterCopyByLocale = {
  en: {
    eyebrow: "AI makeup try-on",
    prefix: "I tried",
    subtext: "A real makeup preview made for my face.",
    rewardTop: "Share daily",
    rewardBottom: "get +1 try-on",
  },
  "zh-CN": {
    eyebrow: "AI 在线试妆",
    prefix: "我试了",
    subtext: "用我的脸生成真实妆容预览。",
    rewardTop: "每日分享",
    rewardBottom: "+1 次试妆",
  },
  "zh-TW": {
    eyebrow: "AI 線上試妝",
    prefix: "我試了",
    subtext: "用我的臉生成真實妝容預覽。",
    rewardTop: "每日分享",
    rewardBottom: "+1 次試妝",
  },
  "de-DE": {
    eyebrow: "AI Make-up-Try-on",
    prefix: "Ich teste",
    subtext: "Eine echte Make-up-Vorschau für mein Gesicht.",
    rewardTop: "Täglich teilen",
    rewardBottom: "+1 Try-on erhalten",
  },
  "fr-FR": {
    eyebrow: "Essai maquillage IA",
    prefix: "J'essaie",
    subtext: "Un aperçu maquillage créé pour mon visage.",
    rewardTop: "Partager chaque jour",
    rewardBottom: "+1 essai offert",
  },
  "ja-JP": {
    eyebrow: "AIメイク試着",
    prefix: "試したルック",
    subtext: "自分の顔に合わせたリアルなメイクプレビュー。",
    rewardTop: "毎日シェア",
    rewardBottom: "+1回試着",
  },
  "ko-KR": {
    eyebrow: "AI 메이크업 체험",
    prefix: "체험한 룩",
    subtext: "내 얼굴에 맞춘 실제 메이크업 미리보기.",
    rewardTop: "매일 공유",
    rewardBottom: "+1회 체험",
  },
  "es-ES": {
    eyebrow: "Prueba de maquillaje IA",
    prefix: "Probé",
    subtext: "Una vista previa de maquillaje hecha para mi rostro.",
    rewardTop: "Comparte a diario",
    rewardBottom: "+1 prueba gratis",
  },
  "es-419": {
    eyebrow: "Prueba de maquillaje IA",
    prefix: "Probé",
    subtext: "Una vista previa de maquillaje hecha para mi rostro.",
    rewardTop: "Comparte a diario",
    rewardBottom: "+1 prueba gratis",
  },
  "pt-BR": {
    eyebrow: "Teste de maquiagem IA",
    prefix: "Testei",
    subtext: "Uma prévia de maquiagem feita para o meu rosto.",
    rewardTop: "Compartilhe todo dia",
    rewardBottom: "+1 teste grátis",
  },
} satisfies Record<
  string,
  {
    eyebrow: string;
    prefix: string;
    subtext: string;
    rewardTop: string;
    rewardBottom: string;
  }
>;
const posterCopy =
  posterCopyByLocale[currentLocale as keyof typeof posterCopyByLocale] ??
  posterCopyByLocale.en;
---

<BaseLayout
  title={copy.title}
  description={copy.description}
  robots="noindex,follow"
>
  <div class="share-route">
    <nav class="share-nav" aria-label={copy.navLabel}>
      <a class="share-nav-brand" href={homeHref}>
        <img
          class="brand-logo-img"
          src="/images/logo-ui.webp"
          alt=""
          aria-hidden="true"
          width="34"
          height="34"
        />
        <span>AI Beauty Stylist</span>
      </a>
      <div class="share-nav-links">
        <a href={homeHref}>{copy.navHome}</a>
        <a href={discoverHref}>{copy.navDiscover}</a>
        <a href={tryOnHref} data-tryon-link data-tryon-nav-label>{copy.navTryOn}</a>
      </div>
      <a class="btn btn-primary share-nav-cta" href={tryOnHref} data-tryon-link>
        {copy.navTryCta}
      </a>
    </nav>

    <header class="share-mobile-nav" aria-label={copy.navLabel}>
      <a class="share-mobile-home" href={homeHref} aria-label={copy.navHome}>
        <svg viewBox="0 0 24 24" aria-hidden="true">
          <path d="M3 10.8 12 3l9 7.8"></path>
          <path d="M5.5 10.2V21h13V10.2"></path>
          <path d="M9.5 21v-6h5v6"></path>
        </svg>
      </a>
      <strong>{copy.navTitle}</strong>
      <a class="share-mobile-tryon" href={tryOnHref} data-tryon-link>{copy.navTryShort}</a>
    </header>

    <main class="share-page">
      <article
        class="share-card share-poster"
        aria-label={copy.cardAria}
        id="share-card"
      >
        <div class="share-poster-media" id="share-image-area">
          <div class="share-loading">{copy.cardLoading}</div>
        </div>
        <footer class="share-poster-copy">
          <div class="share-poster-meta">
            <span class="share-poster-chip">{copy.after}</span>
            <span>{posterCopy.eyebrow}</span>
          </div>
          <h1>
            <strong id="share-look-name">{copy.loading}</strong>
          </h1>
          <p>{posterCopy.subtext}</p>
          <div class="share-poster-footer">
            <a class="share-poster-brand" href={homeHref}>
              <img
                class="brand-logo-img share-brand-logo"
                src="/images/logo-ui.webp"
                alt=""
                aria-hidden="true"
              />
              <span>AI Beauty Stylist</span>
            </a>
            <span class="share-poster-url">aibeautystylist.com</span>
          </div>
        </footer>
      </article>

      <!-- 操作按钮 -->
      <div class="share-actions" id="share-actions">
        <button class="btn btn-primary" id="share-download-btn">
          ↓&nbsp; {copy.download}</button
        >
        <button class="btn" id="share-web-btn">↗&nbsp; {copy.share}</button>
        <button class="btn" id="share-copy-btn">⌘&nbsp; {copy.copyLink}</button>
        <button class="btn btn-outline-rose" id="share-reward-btn" hidden>
          +&nbsp; {copy.reward}</button
        >
      </div>

      <!-- 无结果时展示 -->
      <div class="panel share-empty" id="share-empty" hidden>
        <h2>{copy.emptyTitle}</h2>
        <p>{copy.emptyBody}</p>
        <div class="share-empty-actions">
          <a class="btn btn-primary" href={tryOnHref} data-tryon-link>{copy.emptyCta}</a>
          <a class="btn" href={homeHref}>{copy.emptyHome}</a>
        </div>
      </div>
    </main>
  </div>
</BaseLayout>

<script
  is:inline
  define:vars={{ copy, posterCopy, shareBaseHref, shareCardHref, tryOnHref, tryOnProHref, tryOnPremiumHref }}
>
  (function () {
    const params = new URLSearchParams(location.search);
    const jobId = params.get("jobId");
    const lookNameEl = document.getElementById("share-look-name");
    const imageArea = document.getElementById("share-image-area");
    const emptyEl = document.getElementById("share-empty");
    const actionsEl = document.getElementById("share-actions");
    const cardEl = document.getElementById("share-card");
    const rewardBtn = document.getElementById("share-reward-btn");
    const downloadBtn = document.getElementById("share-download-btn");
    const webShareBtn = document.getElementById("share-web-btn");
    const copyBtn = document.getElementById("share-copy-btn");
    let currentPlan = "free";
    let hasLoadedPlan = false;
    let currentJob = null;
    let publicShareUrl;

    if (!jobId) {
      cardEl.hidden = true;
      actionsEl.hidden = true;
      emptyEl.hidden = false;
      return;
    }

    function shareClaimStorageKey(value) {
      return "abs:share-claim:" + value;
    }

    function getStoredShareClaim() {
      try {
        const raw = sessionStorage.getItem(shareClaimStorageKey(jobId));
        if (!raw) return null;
        const claim = JSON.parse(raw);
        if (!claim?.claimToken || !claim?.expiresAt) return null;
        if (claim.expiresAt <= new Date().toISOString()) {
          sessionStorage.removeItem(shareClaimStorageKey(jobId));
          return null;
        }
        return claim;
      } catch {
        return null;
      }
    }

    function storeShareClaim(claim) {
      try {
        sessionStorage.setItem(
          shareClaimStorageKey(jobId),
          JSON.stringify(claim),
        );
      } catch {
        /* ignore private storage failures */
      }
    }

    async function createShareClaim(method) {
      const res = await fetch("/api/shares/intent", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ jobId, method }),
      });
      const data = await res.json();
      if (!res.ok || !data.ok || !data.data?.claimToken) {
        throw new Error(data?.error?.message || "SHARE_INTENT_FAILED");
      }
      storeShareClaim(data.data);
      refreshRewardButtonState();
      return data.data;
    }

    async function createPublicShareUrl() {
      if (publicShareUrl) return publicShareUrl;
      const res = await fetch("/api/shares", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ jobId }),
      });
      const data = await res.json();
      if (!res.ok || !data.ok || !data.data?.sourceCode) {
        throw new Error(data?.error?.message || "SHARE_FAILED");
      }
      const base = String(shareBaseHref || "/share").replace(/\/+$/, "");
      publicShareUrl = new URL(
        `${base}/${encodeURIComponent(data.data.sourceCode)}`,
        location.origin,
      ).toString();
      return publicShareUrl;
    }

    function refreshRewardButtonState() {
      if (!rewardBtn) return;
      if (!hasLoadedPlan || isPaidPlan()) {
        rewardBtn.hidden = true;
        rewardBtn.disabled = true;
        return;
      }
      rewardBtn.hidden = false;
      rewardBtn.disabled = !getStoredShareClaim();
    }

    refreshRewardButtonState();
    loadSessionPlan();

    function normalizePlan(plan) {
      return plan === "premium" ? "premium" : plan === "pro" ? "pro" : "free";
    }

    function isPaidPlan() {
      return currentPlan === "pro" || currentPlan === "premium";
    }

    async function loadSessionPlan() {
      try {
        const res = await fetch("/api/session");
        const data = await res.json();
        if (data.ok) currentPlan = normalizePlan(data.data?.plan);
      } catch {
        currentPlan = "free";
      }
      hasLoadedPlan = true;
      updateTryOnLinks();
      updateDownloadButtonLabel();
      refreshRewardButtonState();
    }

    function updateTryOnLinks() {
      const targetHref =
        currentPlan === "premium"
          ? tryOnPremiumHref
          : currentPlan === "pro"
            ? tryOnProHref
            : tryOnHref;
      document.querySelectorAll("[data-tryon-link]").forEach(function (el) {
        el.href = targetHref;
      });
      if (isPaidPlan()) {
        var navLabel = document.querySelector("[data-tryon-nav-label]");
        if (navLabel) navLabel.textContent = copy.navTryOnPaid;
      }
    }

    function updateDownloadButtonLabel() {
      if (!downloadBtn) return;
      downloadBtn.textContent =
        "↓  " +
        (isPaidPlan() ? copy.cleanDownload || copy.download : copy.download);
    }

    fetch("/api/tryon-jobs/" + jobId)
      .then((r) => r.json())
      .then((data) => {
        if (!data.ok || data.data.status !== "succeeded") {
          cardEl.hidden = true;
          actionsEl.hidden = true;
          emptyEl.hidden = false;
          return;
        }
        const job = data.data;
        currentJob = job;
        if (lookNameEl)
          lookNameEl.textContent = job.lookTitle || copy.defaultLook;
        if (imageArea && job.resultImage) {
          const lookTitle = job.lookTitle || copy.defaultLook;
          const resultAlt = lookTitle + copy.resultAltSuffix;
          imageArea.innerHTML = `
            <img src="${escapeHtml(job.resultImage)}" alt="${escapeHtml(resultAlt)}" loading="eager" data-share-result-img />
          `;
        }
        createPublicShareUrl().catch(() => {});
      })
      .catch(() => {
        cardEl.hidden = true;
        actionsEl.hidden = true;
        emptyEl.hidden = false;
      });

    function escapeHtml(value) {
      return String(value ?? "")
        .replaceAll("&", "&amp;")
        .replaceAll("<", "&lt;")
        .replaceAll(">", "&gt;")
        .replaceAll('"', "&quot;")
        .replaceAll("'", "&#39;");
    }

    function loadImage(src) {
      return new Promise((resolve, reject) => {
        const img = new Image();
        img.onload = () => resolve(img);
        img.onerror = reject;
        img.src = src;
      });
    }

    function roundedRect(ctx, x, y, width, height, radius) {
      const r = Math.min(radius, width / 2, height / 2);
      ctx.beginPath();
      ctx.moveTo(x + r, y);
      ctx.lineTo(x + width - r, y);
      ctx.quadraticCurveTo(x + width, y, x + width, y + r);
      ctx.lineTo(x + width, y + height - r);
      ctx.quadraticCurveTo(x + width, y + height, x + width - r, y + height);
      ctx.lineTo(x + r, y + height);
      ctx.quadraticCurveTo(x, y + height, x, y + height - r);
      ctx.lineTo(x, y + r);
      ctx.quadraticCurveTo(x, y, x + r, y);
      ctx.closePath();
    }

    function drawImageCover(ctx, img, x, y, width, height, focusY = 0.38) {
      const scale = Math.max(
        width / img.naturalWidth,
        height / img.naturalHeight,
      );
      const sw = width / scale;
      const sh = height / scale;
      const sx = Math.max(0, (img.naturalWidth - sw) / 2);
      const sy = Math.max(0, (img.naturalHeight - sh) * focusY);
      ctx.drawImage(img, sx, sy, sw, sh, x, y, width, height);
    }

    function drawWrappedText(ctx, text, x, y, maxWidth, lineHeight, maxLines) {
      const source = String(text || "");
      const hasSpaces = /\s/.test(source);
      const words = hasSpaces ? source.split(/\s+/) : Array.from(source);
      const joiner = hasSpaces ? " " : "";
      const lines = [];
      let line = "";
      for (const word of words) {
        const next = line ? line + joiner + word : word;
        if (ctx.measureText(next).width > maxWidth && line) {
          lines.push(line);
          line = word;
        } else {
          line = next;
        }
        if (lines.length >= maxLines) break;
      }
      if (line && lines.length < maxLines) lines.push(line);
      lines.forEach((entry, index) =>
        ctx.fillText(entry, x, y + index * lineHeight),
      );
      return y + lines.length * lineHeight;
    }

    async function createShareCardBlob() {
      const imgEl = imageArea?.querySelector("[data-share-result-img]");
      const resultSrc = imgEl?.src || currentJob?.resultImage;
      if (!resultSrc) return null;
      const resultImage = await loadImage(resultSrc);
      const logoImage = await loadImage("/images/logo-256.png").catch(
        () => null,
      );
      const canvas = document.createElement("canvas");
      canvas.width = 1080;
      canvas.height = 1440;
      const ctx = canvas.getContext("2d");
      if (!ctx) return null;

      ctx.fillStyle = "#fff8fb";
      ctx.fillRect(0, 0, canvas.width, canvas.height);

      drawImageCover(ctx, resultImage, 0, 0, 1080, 1138, 0.34);

      const imageFade = ctx.createLinearGradient(0, 1046, 0, 1138);
      imageFade.addColorStop(0, "rgba(255,248,251,0)");
      imageFade.addColorStop(1, "rgba(255,248,251,0.82)");
      ctx.fillStyle = imageFade;
      ctx.fillRect(0, 1046, 1080, 92);

      ctx.fillStyle = "#fff8fb";
      ctx.fillRect(0, 1138, 1080, 302);
      ctx.strokeStyle = "rgba(130,72,92,0.14)";
      ctx.lineWidth = 2;
      ctx.beginPath();
      ctx.moveTo(64, 1138);
      ctx.lineTo(1016, 1138);
      ctx.stroke();

      ctx.textBaseline = "alphabetic";
      ctx.textAlign = "left";

      ctx.font = "800 23px Inter, Arial, sans-serif";
      const afterWidth = Math.max(118, ctx.measureText(copy.after).width + 56);
      ctx.fillStyle = "rgba(185,47,98,0.1)";
      roundedRect(ctx, 64, 1156, afterWidth, 40, 20);
      ctx.fill();
      ctx.strokeStyle = "rgba(185,47,98,0.28)";
      ctx.stroke();
      ctx.fillStyle = "#a82e5c";
      ctx.textAlign = "center";
      ctx.fillText(copy.after, 64 + afterWidth / 2, 1184);
      ctx.textAlign = "left";

      ctx.fillStyle = "rgba(83,55,68,0.72)";
      ctx.font = "800 23px Inter, Arial, sans-serif";
      ctx.fillText(posterCopy.eyebrow, 64 + afterWidth + 24, 1184);

      const lookTitle = currentJob?.lookTitle || copy.defaultLook;
      const compactTitle = lookTitle.length > 24;
      ctx.fillStyle = "#2d2028";
      ctx.font = compactTitle
        ? "900 50px Inter, Arial, sans-serif"
        : "900 58px Inter, Arial, sans-serif";
      const nextY = drawWrappedText(
        ctx,
        lookTitle,
        64,
        compactTitle ? 1236 : 1250,
        952,
        compactTitle ? 50 : 56,
        2,
      );

      ctx.fillStyle = "rgba(83,55,68,0.86)";
      ctx.font = "700 24px Inter, Arial, sans-serif";
      drawWrappedText(ctx, posterCopy.subtext, 64, nextY - 14, 760, 28, 2);

      const brandText = "AI Beauty Stylist";
      ctx.font = "900 32px Inter, Arial, sans-serif";
      if (logoImage) {
        ctx.save();
        roundedRect(ctx, 64, 1344, 52, 52, 26);
        ctx.clip();
        ctx.drawImage(logoImage, 64, 1344, 52, 52);
        ctx.restore();
      }
      ctx.fillStyle = "#2d2028";
      ctx.fillText(brandText, 132, 1382);

      ctx.save();
      ctx.textAlign = "left";
      ctx.fillStyle = "rgba(168,46,92,0.82)";
      ctx.font = "800 26px Inter, Arial, sans-serif";
      const websiteText = "aibeautystylist.com";
      const websiteLetterSpacing = 1.45;
      const websiteWidth =
        ctx.measureText(websiteText).width +
        Math.max(0, websiteText.length - 1) * websiteLetterSpacing;
      let websiteCursorX = 1016 - websiteWidth;
      for (const char of websiteText) {
        ctx.fillText(char, websiteCursorX, 1382);
        websiteCursorX += ctx.measureText(char).width + websiteLetterSpacing;
      }
      ctx.restore();

      return new Promise((resolve) => {
        canvas.toBlob((blob) => resolve(blob), "image/png", 0.95);
      });
    }

    function downloadBlob(blob, filename) {
      if (!blob) return;
      const url = URL.createObjectURL(blob);
      const a = document.createElement("a");
      a.href = url;
      a.download = filename;
      a.click();
      setTimeout(() => URL.revokeObjectURL(url), 1000);
    }

    function downloadCleanResultImage() {
      const img = imageArea?.querySelector("[data-share-result-img]");
      if (!img?.src) return;
      const a = document.createElement("a");
      a.href = img.src;
      a.download = "ai-beauty-stylist-result.png";
      a.click();
    }

    if (downloadBtn) {
      downloadBtn.addEventListener("click", async () => {
        try {
          if (isPaidPlan()) {
            downloadCleanResultImage();
            return;
          }
          const blob = await createShareCardBlob();
          downloadBlob(blob, "ai-beauty-stylist-share-card.png");
        } catch {
          /* ignore */
        }
      });
    }

    if (webShareBtn) {
      webShareBtn.addEventListener("click", async () => {
        try {
          const shareUrl = await createPublicShareUrl();
          if (navigator.share) {
            const sharePayload = {
              title: copy.shareTitle,
              text: copy.shareText,
              url: shareUrl,
            };
            const blob = await createShareCardBlob();
            const file =
              blob &&
              new File([blob], "ai-beauty-stylist-share-card.png", {
                type: "image/png",
              });
            if (file && navigator.canShare?.({ files: [file] })) {
              try {
                await navigator.share({ ...sharePayload, files: [file] });
              } catch {
                await navigator.share(sharePayload);
              }
            } else {
              await navigator.share(sharePayload);
            }
            await createShareClaim("web_share");
          } else if (navigator.clipboard?.writeText) {
            await navigator.clipboard.writeText(shareUrl);
            await createShareClaim("copy_link");
            webShareBtn.textContent = copy.shareCopied;
            setTimeout(() => {
              webShareBtn.textContent = "↗  " + copy.share;
            }, 2000);
          }
        } catch {
          /* user cancelled or sharing unavailable */
        }
      });
    }

    if (copyBtn) {
      copyBtn.addEventListener("click", async () => {
        if (!navigator.clipboard?.writeText) return;
        try {
          const shareUrl = await createPublicShareUrl();
          await navigator.clipboard.writeText(shareUrl);
        } catch {
          return;
        }
        try {
          await createShareClaim("copy_link");
        } catch {
          /* keep copy action best-effort */
        }
        copyBtn.textContent = copy.copied;
        setTimeout(() => {
          copyBtn.textContent = "⌘  " + copy.copyLink;
        }, 2000);
      });
    }

    if (rewardBtn) {
      rewardBtn.addEventListener("click", async () => {
        try {
          const claim = getStoredShareClaim();
          if (!claim) {
            refreshRewardButtonState();
            return;
          }
          rewardBtn.disabled = true;
          const res = await fetch("/api/shares/reward", {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({
              jobId,
              shareClaimToken: claim.claimToken,
            }),
          });
          const data = await res.json();
          if (data.ok && data.data.rewarded) {
            rewardBtn.textContent = copy.rewardSuccess;
            rewardBtn.classList.add("btn-primary");
          } else {
            rewardBtn.textContent =
              "i " + (data.data?.reason || copy.rewardAlready);
          }
        } catch {
          rewardBtn.textContent = copy.rewardFailed;
          rewardBtn.disabled = false;
        }
      });
    }
  })();
</script>

<style>
  .share-route {
    min-height: 100dvh;
    background: var(--page-surface-alt);
  }
  .share-nav {
    position: sticky;
    top: 0;
    z-index: 20;
    display: grid;
    min-height: 64px;
    grid-template-columns: minmax(0, 1fr) auto minmax(0, 1fr);
    align-items: center;
    gap: 20px;
    padding: 10px clamp(24px, 5vw, 64px);
    border-bottom: 1px solid color-mix(in srgb, var(--line) 76%, transparent);
    background: color-mix(in srgb, var(--bg-elevated) 92%, transparent);
    backdrop-filter: blur(16px);
  }
  .share-nav-brand,
  .share-nav-links {
    display: inline-flex;
    align-items: center;
  }
  .share-nav-brand {
    min-width: 0;
    gap: 8px;
    color: var(--heading);
    font-size: 15px;
    font-weight: 900;
    text-decoration: none;
  }
  .share-nav-brand img {
    width: 34px;
    height: 34px;
    flex: 0 0 34px;
  }
  .share-nav-links {
    justify-content: center;
    gap: 6px;
  }
  .share-nav-links a {
    display: inline-flex;
    min-height: 38px;
    align-items: center;
    padding: 0 12px;
    border-radius: 999px;
    color: var(--nav-link);
    font-size: 13px;
    font-weight: 800;
    text-decoration: none;
  }
  .share-nav-links a:hover,
  .share-nav-links a:focus-visible {
    background: var(--rose-soft);
    color: var(--rose);
    outline: 0;
  }
  .share-nav-cta {
    min-height: 38px;
    justify-self: end;
    padding-inline: 15px;
  }
  .share-mobile-nav {
    display: none;
  }
  :global(.share-page) {
    min-height: calc(100dvh - 64px);
  }
  .share-poster {
    position: relative;
    isolation: isolate;
    width: min(100%, 560px);
    aspect-ratio: 3 / 4;
    display: grid;
    grid-template-rows: minmax(0, 1fr) auto;
    overflow: hidden;
    border: 0;
    border-radius: 28px;
    background: #fff8fb;
    color: #2d2028;
    box-shadow:
      0 28px 90px rgba(76, 38, 54, 0.18),
      0 2px 0 rgba(255, 255, 255, 0.65) inset;
  }
  .share-poster-media {
    position: relative;
    display: grid;
    overflow: hidden;
    place-items: center;
    background: #efe7ea;
  }
  :global(.share-poster-media img) {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center 42%;
    filter: saturate(1.03) contrast(1.02);
  }
  .share-poster-copy {
    position: relative;
    display: flex;
    min-width: 0;
    flex-direction: column;
    gap: clamp(4px, 1.1vw, 8px);
    padding: clamp(12px, 3vw, 21px) clamp(20px, 5.2vw, 36px)
      clamp(12px, 3vw, 21px);
    border-top: 1px solid rgba(130, 72, 92, 0.14);
    background: linear-gradient(180deg, #fff8fb 0%, #fff 100%);
  }
  .share-poster-meta {
    display: flex;
    align-items: center;
    gap: 10px;
    color: rgba(83, 55, 68, 0.72);
    font-size: clamp(11px, 2.7vw, 14px);
    font-weight: 850;
    line-height: 1.2;
    white-space: nowrap;
  }
  .share-poster-chip {
    display: inline-flex;
    min-height: 28px;
    align-items: center;
    padding: 0 13px;
    border: 1px solid rgba(185, 47, 98, 0.26);
    border-radius: 999px;
    background: rgba(185, 47, 98, 0.08);
    color: #a82e5c;
  }
  .share-poster-copy h1 {
    max-width: 100%;
    margin: 0;
    color: #2d2028;
    font-size: clamp(27px, 7.2vw, 46px);
    font-weight: 900;
    letter-spacing: 0;
    line-height: 1.02;
    text-wrap: balance;
  }
  .share-poster-copy h1 strong {
    min-width: 0;
    font-weight: inherit;
  }
  .share-poster-copy > p {
    max-width: 48ch;
    margin: 0;
    color: rgba(83, 55, 68, 0.86);
    font-size: clamp(11px, 2.7vw, 15px);
    font-weight: 700;
    line-height: 1.18;
  }
  .share-poster-footer {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
    margin-top: clamp(1px, 0.7vw, 6px);
  }
  .share-poster-brand {
    display: inline-flex;
    min-width: 0;
    align-items: center;
    gap: 10px;
    color: #2d2028;
    font-size: clamp(15px, 3.7vw, 21px);
    font-weight: 900;
    text-decoration: none;
  }
  .share-poster-brand img {
    width: clamp(34px, 8.2vw, 46px);
    height: clamp(34px, 8.2vw, 46px);
    flex: 0 0 auto;
    border-radius: 999px;
  }
  .share-poster-brand span {
    min-width: 0;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }
  .share-poster-url {
    flex: 0 1 auto;
    min-width: 0;
    overflow: hidden;
    color: rgba(168, 46, 92, 0.82);
    font-size: clamp(13px, 3.1vw, 18px);
    font-weight: 800;
    letter-spacing: 0.045em;
    line-height: 1;
    text-align: right;
    text-overflow: ellipsis;
    white-space: nowrap;
  }
  .share-actions {
    display: flex;
    gap: 10px;
    justify-content: center;
    flex-wrap: wrap;
    margin: 24px auto;
    max-width: 600px;
  }
  .share-loading {
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 200px;
    color: var(--muted);
  }
  .share-empty {
    max-width: min(100%, 560px);
    margin: 0 auto;
    padding: 40px;
    text-align: center;
  }
  .share-empty-actions {
    display: flex;
    justify-content: center;
    gap: 10px;
    flex-wrap: wrap;
    margin-top: 18px;
  }
  @media (max-width: 700px) {
    .share-nav {
      display: none;
    }
    .share-mobile-nav {
      position: sticky;
      top: 0;
      z-index: 20;
      display: grid;
      min-height: 56px;
      grid-template-columns: 44px minmax(0, 1fr) auto;
      align-items: center;
      gap: 8px;
      padding: 6px max(12px, env(safe-area-inset-left)) 6px
        max(12px, env(safe-area-inset-right));
      border-bottom: 1px solid color-mix(in srgb, var(--line) 72%, transparent);
      background: color-mix(in srgb, var(--bg-elevated) 94%, transparent);
      backdrop-filter: blur(16px);
    }
    .share-mobile-nav strong {
      min-width: 0;
      overflow: hidden;
      color: var(--heading);
      font-size: 16px;
      font-weight: 900;
      text-align: center;
      text-overflow: ellipsis;
      white-space: nowrap;
    }
    .share-mobile-home,
    .share-mobile-tryon {
      display: inline-flex;
      min-height: 44px;
      align-items: center;
      justify-content: center;
      border-radius: 999px;
      text-decoration: none;
    }
    .share-mobile-home {
      width: 44px;
      color: var(--heading);
    }
    .share-mobile-home svg {
      width: 21px;
      height: 21px;
      fill: none;
      stroke: currentColor;
      stroke-linecap: round;
      stroke-linejoin: round;
      stroke-width: 2;
    }
    .share-mobile-tryon {
      padding: 0 12px;
      background: var(--rose);
      color: #fff;
      font-size: 12px;
      font-weight: 900;
      white-space: nowrap;
    }
    :global(.share-page) {
      min-height: calc(100dvh - 56px);
      padding-top: 0;
    }
    .share-poster {
      width: 100%;
      grid-template-rows: minmax(0, 1fr) auto;
      border-radius: 22px;
    }
    .share-poster-copy {
      gap: 3px;
      padding: 8px 18px 9px;
    }
    .share-poster-meta {
      gap: 6px;
      font-size: 10px;
    }
    .share-poster-chip {
      min-height: 21px;
      padding: 0 9px;
    }
    .share-poster-copy h1 {
      font-size: clamp(21px, 5.9vw, 29px);
    }
    .share-poster-copy > p {
      font-size: 10.4px;
      line-height: 1.08;
    }
    .share-poster-brand {
      gap: 6px;
      font-size: 13.8px;
    }
    .share-poster-brand img {
      width: 26px;
      height: 26px;
    }
    .share-poster-url {
      font-size: 12.8px;
      letter-spacing: 0.042em;
    }
    .share-empty {
      margin: 16px;
      padding: 28px 18px;
    }
    .share-empty-actions {
      display: grid;
    }
  }
</style>
