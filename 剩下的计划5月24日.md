# 剩余建设计划 — 2026-05-24

> **跳过**：A1 Affiliate SKU 系统（账号未申请，待后续补）

---

## 今日执行清单

### Phase A — 变现闭环（跳过 A1）

- [x] **A2. 前端门控渲染** ✅ 已存在（审计时发现已完整实现）
  - [x] 后端 `trimResultByTier()` 裁剪 looks 和 tutorial steps
  - [x] 前端 `applyGatedInfo()` 显示升级横幅 + 教程锁卡
  - [x] CSS 样式 `.tryon-gated-banner` + `.tryon-tutorial-lock` 完备

- [x] **A3. Before/After 分享海报** ✅
  - [x] shareCardGenerator.ts 新增 `makeupResultUrl` 字段
  - [x] `drawBeforeAfter()` 绘制双圆形对比图 + "Before/After" 标签
  - [x] `drawCircleImage()` + `loadImage()` 辅助函数

- [x] **A4. 首页灵感图可点击** ✅ 已存在 + 增强
  - [x] 已有 `<a href="/try-on?scenario=xxx">` 包裹
  - [x] 新增 "Try this look →" hover 浮层（`.look-card-hover-cta`）

---

### Phase B — 视觉与内容填充

- [x] **B1. 图片批量生成脚本** ✅
  - [x] `scripts/generate-images.ts` 已存在（9 张核心图）
  - [x] 新增 `scripts/generate-images-extended.ts`（18 张扩展图）
  - [x] 场景入口图 6 张（面试/婚礼嘉宾/日常/Zoom/减龄/单眼皮）
  - [x] 信任图 12 张（4 肤色 × 3 脸型）

- [x] **B2. 风格型 SEO 页** ✅
  - [x] `src/data/stylePages.ts` — 6 个风格页数据
  - [x] `src/pages/styles/[slug].astro` — 动态路由 + Schema.org
  - [x] `makeup-guides.astro` 新增 "By Style" 区块
  - [x] `sitemap.xml` 新增 6 条 URL

- [x] **B3. 教程视觉增强** ✅
  - [x] `src/lib/tutorialVisuals.ts` — 面部区域 + 工具映射引擎
  - [x] 7 个面部区域 SVG 图标 + 8 个工具 SVG 图标
  - [x] 步骤卡 badge 展示（zone + tool）
  - [x] 步骤卡 "Done / Need help / Simpler version" 按钮

---

### Phase C — 架构预埋

- [x] **C1. i18n 架构预埋** ✅
  - [x] `src/locales/en.json` — 完整语言包（8 模块 80+ 条文案）
  - [x] `src/lib/i18n.ts` — `t()` 翻译函数 + locale 工具
  - [x] BaseLayout 预留 hreflang 标签（en + x-default）
  - [ ] 渐进迁移：各组件调用 `t()` 替代硬编码（Phase 2）

- [x] **C2. Sitemap 自动生成** ✅
  - [x] 安装 `@astrojs/sitemap` 集成
  - [x] `astro.config.mjs` 中配置（过滤 /api/ 路由）
  - [x] 手动 sitemap.xml 保留为 fallback

- [x] **C3. AI 免责 + 香槟金点缀** ✅
  - [x] TryOnStudio.astro 结果区新增 `.tryon-ai-disclaimer`
  - [x] CSS 变量 `--accent-gold: #c5a55a` + `--accent-gold-soft: #f9f3e3`
  - [x] 免责条样式：金色背景 + 斜体

- [x] **C4. 导航架构调整** ✅
  - [x] 五入口：Discover / Try On / Tutorials / Beauty Kit / Membership

---

## 执行顺序

1. A2 前端门控 → 2. A4 灵感图可点击 → 3. A3 分享海报 → 4. B2 风格SEO页 → 5. B3 教程增强 → 6. C1 i18n预埋 → 7. C2 Sitemap → 8. C3 免责 → 9. C4 导航 → 10. B1 图片生成（最耗时，放最后批量跑）

---

## 延后事项

| 事项 | 原因 | 预计时间 |
|------|------|---------|
| A1 Affiliate SKU 系统 | 账号未申请 | 账号下来后 3-4 天 |
| B1 图片实际生成 | 脚本写好后需批量调用 API | 脚本完成后 1-2 天跑完 |
