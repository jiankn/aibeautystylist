# 项目全局规则 (Project Global Rules)

## 1. 版本控制与 Git 管理
- **重大代码变动规范**：任何涉及项目代码的大变动（例如：重构核心逻辑、接入真实 AI 服务、修改数据库 Schema、重构 API 路由等），**必须**使用 Git 进行版本控制与管理。
- **改动前检查**：在进行大变动之前，必须先使用 `git status` 确认当前工作区是干净的。如果有未提交的改动，应先进行提交（Commit）或暂存（Stash）。
- **分支策略**：建议为较大的新功能、重构或独立模块创建单独的特性分支（Feature Branch）进行开发，验证无误后再合并回主分支。
- **提交规范**：每次完成阶段性工作或重要变更后，必须进行 Git 提交。Commit Message 必须清晰明了，概括本次变动的内容与目的（建议使用简体中文或符合 Angular 规范的 Commit 格式，如 `feat: 接入真实 AI 诊断服务`）。

## 2. 开发与技术栈规范
- **框架与构建**：本项目基于 Astro 6 + Cloudflare 全家桶（Workers/D1/R2/KV）构建，遵循 SSR 和零 JS 岛屿架构。
- **多语言要求**：与用户交流及撰写说明文档时，必须始终遵循**简体中文 (Simplified Chinese)**。
- **代码注释**：默认使用简体中文为新增或修改的代码编写注释，保持注释清晰、准确。
